#include <stdio.h>

int main() {
    // type of data type in c 
    /*
    primitive data types : inbuilt data types : int , char , float , double , void 
    derived data types  : derived from primitive or inbuilt data types : array , pointer , function 
    user defind data types : defind by the user himself : structure , union , enum 
    */
    
    int no = 11;
    printf("Entered number is : %d",no);
    printf("d",sizeof(no));
    
    return 0;
}
